```shell
docker stack ls 
docker service ls
docker service ps

docker stack deploy --compose-file docker-compose.yml app_stack

docker service logs

docker container ps -a
docker rm -f
docker container ps -a

docker stack rm
```